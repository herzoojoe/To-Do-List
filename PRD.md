# To-Do List 앱 PRD (Product Requirements Document)

## 1. 개요

### 1.1 제품 목적
매일 10~20개 수준의 할 일을 관리하는 개인용 To-Do 앱. 설치나 회원가입 없이 브라우저에서 즉시 사용할 수 있고, 새로고침이나 브라우저 재실행 후에도 데이터가 유지되는 가벼운 도구.

### 1.2 대상 사용자
- 본인(1인 사용) 전용
- 하루 단위로 할 일을 관리하며, 업무/개인/공부 등으로 구분해서 보고 싶은 사용자

### 1.3 핵심 가치
- **빠른 진입**: 파일 하나 열면 바로 사용 가능
- **데이터 유지**: 새로고침해도 목록이 사라지지 않음
- **가벼움**: 프레임워크·빌드 과정 없는 순수 JS

---

## 2. 범위

### 2.1 In Scope (이번 버전에 포함)
| 기능 | 설명 |
|---|---|
| 할 일 추가 | 텍스트 입력 후 목록에 추가 |
| 할 일 수정 | 기존 항목의 텍스트/카테고리 수정 |
| 할 일 삭제 | 개별 항목 삭제 (실수 방지용 확인 절차 포함) |
| 완료 체크 | 체크박스로 완료/미완료 토글, 완료 항목은 시각적으로 구분(취소선 등) |
| 카테고리 분류 | 업무 / 개인 / 공부 3종 고정 카테고리, 항목별 지정 및 필터링 |
| 진행률 보기 | 전체 대비 완료 비율을 숫자(%) 및 프로그레스 바로 표시 |
| 데이터 영속성 | `localStorage` 기반 자동 저장, 새로고침·재접속 시 복원 |
| 다크 모드 | 라이트/다크 테마 토글, 선택한 테마를 `localStorage`에 저장해 재접속 시 유지 |

### 2.2 Out of Scope (이번 버전 제외)
- 회원가입/로그인, 서버 연동, 클라우드 동기화
- 여러 기기 간 데이터 공유
- 날짜별 할 일 분리(예: 캘린더 뷰) — 필요 시 다음 버전 검토
- 알림/리마인더 기능
- 협업(공유, 댓글 등) 기능
- 서브태스크, 태그, 우선순위 등 부가 속성

---

## 3. 사용자 스토리

1. 사용자로서, 오늘 할 일을 빠르게 입력하고 목록에 추가하고 싶다.
2. 사용자로서, 잘못 입력한 할 일의 내용이나 카테고리를 수정하고 싶다.
3. 사용자로서, 더 이상 필요 없는 할 일을 삭제하고 싶다.
4. 사용자로서, 완료한 항목을 체크해서 구분하고 싶다.
5. 사용자로서, 업무/개인/공부별로 필터링해서 보고 싶다.
6. 사용자로서, 오늘 얼마나 진행했는지 한눈에 확인하고 싶다.
7. 사용자로서, 브라우저를 껐다 켜도 입력한 목록이 그대로 남아있길 원한다.
8. 사용자로서, 야간에 눈이 편하도록 다크 모드로 전환하고, 그 설정이 다음 접속 때도 유지되길 원한다.

---

## 4. 기능 상세 요구사항

### 4.1 할 일 추가
- 입력창(텍스트) + 카테고리 선택(드롭다운 또는 버튼: 업무/개인/공부, 기본값 지정) + 추가 버튼
- Enter 키로도 추가 가능
- 빈 텍스트는 추가되지 않도록 검증
- 추가된 항목은 목록 최상단 또는 최하단에 표시 (기본: 최상단)

### 4.2 할 일 수정
- 항목 클릭 또는 수정 아이콘 클릭 시 인라인 편집 모드 전환
- 텍스트, 카테고리 모두 수정 가능
- Enter 또는 확인 버튼으로 저장, Esc로 취소

### 4.3 할 일 삭제
- 항목별 삭제 버튼(아이콘) 제공
- 삭제 시 간단한 확인(예: `confirm()` 또는 실행취소 토스트) — 오삭제 방지

### 4.4 완료 체크
- 항목 좌측 체크박스로 완료 토글
- 완료 항목은 취소선 + 흐림 처리 등으로 시각적 구분
- 완료 항목을 목록 하단으로 정렬하는 옵션 (선택 사항)

### 4.5 카테고리 분류
- 카테고리: 업무 / 개인 / 공부 (3종 고정)
- 카테고리별 색상 태그로 구분
- 상단 필터 탭(전체/업무/개인/공부)으로 목록 필터링

### 4.6 진행률 보기
- "완료 N / 전체 M (X%)" 형태의 텍스트 표시
- 프로그레스 바로 시각화
- 카테고리 필터 적용 시 해당 카테고리 기준으로 진행률 재계산 (전체 진행률과 별도 또는 통합, 택1)

### 4.7 데이터 저장 (영속성)
- 모든 CRUD 동작 시 `localStorage`에 즉시 저장
- 페이지 로드 시 `localStorage`에서 데이터 불러와 렌더링
- 저장 데이터가 없을 경우 빈 목록으로 초기화

### 4.8 다크 모드
- 헤더에 라이트/다크 전환 토글 버튼(🌙/☀️) 배치
- 저장된 테마 설정이 없으면 시스템 설정(`prefers-color-scheme`)을 기본값으로 사용
- 선택한 테마는 `localStorage`에 저장되어 새로고침·재접속 시에도 유지
- 테마 적용은 페이지 렌더링 전에 이루어져 전환 시 깜빡임(FOUC) 없이 표시
- 다크 모드에서도 카테고리 색상 태그, 진행률 바, 텍스트 대비가 명확히 구분되어야 함

---

## 5. 데이터 모델 (예시)

```js
// localStorage key: "todos"
[
  {
    id: "uuid-or-timestamp",
    text: "PRD 작성하기",
    category: "업무", // "업무" | "개인" | "공부"
    completed: false,
    createdAt: "2026-08-22T09:00:00.000Z"
  }
]

// localStorage key: "theme"
"light" // "light" | "dark" — 없으면 시스템 설정(prefers-color-scheme)을 따름
```

---

## 6. 기술 요구사항

- **언어/스택**: 순수 HTML + CSS + Vanilla JavaScript (프레임워크, 빌드 도구 없음)
- **실행 방식**: 로컬 정적 서버로 서빙해서 실행 (권장) — 별도 설치 없이 `python -m http.server 8080` 등으로 구동 후 `http://localhost:8080` 접속
  - ⚠️ `index.html` 파일을 더블클릭해 `file://`로 직접 여는 방식은 지양한다. 브라우저에 따라 CSS/JS 로드나 `localStorage` 접근이 제한될 수 있어 스타일이 깨지거나 기능이 정상 동작하지 않을 수 있음
- **저장소**: `localStorage` (브라우저 내장, 별도 백엔드 불필요)
- **호환성**: 최신 Chrome/Edge 기준 (개인용이므로 광범위한 브라우저 호환성 불필요)
- **반응형**: 데스크톱 우선, 모바일 크기에서도 깨지지 않는 최소한의 반응형 대응

---

## 7. 비기능 요구사항

- 할 일 10~20개 수준에서 지연 없이 즉시 반응 (성능 이슈 없음, 최적화 불필요)
- 코드 구조는 단일 또는 소수의 파일(HTML/CSS/JS)로 단순하게 유지
- 별도 인증/보안 요구사항 없음 (개인 로컬 데이터)

---

## 8. 화면 구성 (와이어프레임 개념)

```
┌─────────────────────────────────────────┐
│  To-Do List                              │
│  진행률: ▓▓▓▓▓▓░░░░ 60% (6/10)            │
├─────────────────────────────────────────┤
│  [입력창........................] [카테고리▾] [추가] │
├─────────────────────────────────────────┤
│  [전체] [업무] [개인] [공부]               │
├─────────────────────────────────────────┤
│  ☑ ~~보고서 제출~~        [업무]   [수정][삭제] │
│  ☐ 운동하기               [개인]   [수정][삭제] │
│  ☐ React 강의 듣기         [공부]   [수정][삭제] │
│  ...                                     │
└─────────────────────────────────────────┘
```

---

## 9. 성공 기준 (Acceptance Criteria)

- [ ] 할 일을 추가하면 즉시 목록에 나타난다
- [ ] 할 일 텍스트/카테고리를 수정하면 반영된다
- [ ] 할 일을 삭제하면 목록에서 사라진다
- [ ] 체크박스 클릭 시 완료 상태가 시각적으로 구분된다
- [ ] 카테고리 필터를 클릭하면 해당 카테고리 항목만 보인다
- [ ] 진행률이 완료/전체 개수에 맞게 정확히 표시된다
- [ ] 새로고침(F5) 후에도 모든 데이터(목록, 완료 상태, 카테고리)가 그대로 유지된다
- [ ] 빌드 과정 없이 로컬 정적 서버 구동만으로 정상 동작한다 (`file://` 직접 열기가 아닌 `http://localhost` 접속 기준)
- [ ] 다크 모드 토글 클릭 시 즉시 테마가 전환된다
- [ ] 새로고침 후에도 선택한 테마가 유지되고, 전환 시 깜빡임이 없다

---

## 10. 향후 고려 사항 (Nice to have, 이번 범위 아님)
- 날짜별 할 일 분리 / 캘린더 뷰
- 우선순위, 마감일 설정
- 데이터 내보내기/가져오기(JSON export/import)
